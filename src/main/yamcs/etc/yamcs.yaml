services:
  # - class: org.yamcs.tse.TseCommander
  - class: org.yamcs.http.HttpServer
    args:
      port: 8090
      address: "127.0.0.1"
      cors:
        allowOrigin: "*"
        allowCredentials: false

dataDir: yamcs-data

instances:
  - oresat0

secretKey: changeme